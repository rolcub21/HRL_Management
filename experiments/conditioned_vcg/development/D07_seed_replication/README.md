# D7 — Matched seed-1/2 replication

Status: seed 2 passed at round 8; seed 1 reached the fixed round-8 terminal but
required the convergence-controlled continuation in D8.

```bash
bash experiments/conditioned_vcg/development/D07_seed_replication/run.sh analyze
```

The wrapper delegates to
`experiments/vcg_v11_conditioned_handling_two_phase_seeds12/`.
