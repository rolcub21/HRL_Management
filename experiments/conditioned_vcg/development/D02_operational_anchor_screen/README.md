# D2 — Operational-anchor screen

Status: rejected development architecture. Exact lambda-zero behavior was
restored, but the high-lambda endpoint increased physical handling.

This directory delegates to the authenticated launcher at
`experiments/vcg_v11_anchored_preference_seed0_85k/`.

```bash
bash experiments/conditioned_vcg/development/D02_operational_anchor_screen/run.sh analyze
```
